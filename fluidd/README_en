Instruction

Put the same level directory file ( Fluidd ) in the root directory of the USB disk

- Copy the files ( fluidd.sh and fluidd.tar ) to the directory ( /usr/data )
```
$ cp /tmp/udisk/sda1/fluidd/* /usr/data/
```

- Install Fluidd, moonraker and nginx
```
$ ./fluidd.sh install
```

- To uninstall `fluidd`, `moonraker` and `nginx`  
```
./fluidd.sh unstall
```

- Into Fluidd, IP with 4408 port (e.g., 192.168.1.1:4408)

__Warning:__ `Monnraker` running for a long time on the K1 series poses a risk of memory overflow.
